# pogo-rankings-builder

Settings are in the comments of build-rankings.py and can be adjusted as you see fit.

setup:

	pip install -r requirements.txt

running the generator:

	python build-rankings.py
